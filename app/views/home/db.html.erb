<h1>AWS Transcribe Converter</h1>

<h2 id="notice"><%= notice %></h2>

<p>컴플리트 된 레슨은 Transcript를 검토해 주세요!</p>

<table id="main" class="table table-striped table-hover table-bordered">
  <thead class="table-dark">
    <tr>
      <th>JOB ID</th>
      <th>Start Time</th>
      <th>Status</th>
      <th>검토하기</th>
      <th>무음 제거</th>
    </tr>
  </thead>

  <tbody>
    <% @joblist .each do |row| %>
      <tr>
        <td><%= row["job_id"] %></td>
        <td><%= row["start_time"] %></td>
        <td >
            <%= row["status"]%>
        </td> 
        <td >
          <% if row["status"]=="COMPLETED" %>
              <%= link_to 'link', home_cmp_path(:jobid => row["job_id"], :userid => row["user_id"], :tutorid => row["tutor_id"], :recom => row["recom"], :aud => row["aud"]), class:"btn btn-outline-dark" %>
          <% end %>
        </td> 
        <td>
        <% if row["status"]=="COMPLETED" %>
            <%= link_to 'link', home_rmsilence_path(:jobid => row["job_id"], :userid => row["user_id"], :tutorid => row["tutor_id"], :recom => row["recom"], :aud => row["aud"]), class:"btn btn-outline-dark" %>
        <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>


